# 项目交接文档 (Handoff)

> 更新时间: 2026-08-23
> 项目: 基于STM32F103C8T6的智能循迹小车
> 代码位置: `project/code/smart/` (Keil MDK工程, Target: f103)
> 最新提交: `3d868ca`

## 一、项目目标

- **硬件平台**: STM32F103C8T6 + D153C双路TB6612电机驱动板 + 亚博八路蓝光数字灰度传感器
- **功能目标**: 白底黑线循迹小车, 差速转向(无舵机, 靠左右轮速差转弯)
- **用途**: 电子竞赛备赛 + 嵌入式方向学习(目标: 嵌入式硬件工程师实习)

## 二、当前任务

灰度传感器8路已全部验证通过(2026-08-23), 电机正反转开环测试正常。
**正处于"传感器就绪 → 开始写循迹控制代码"的节点**, 尚未动笔。

## 三、已完成内容

### 1. 电机驱动 (BSP/bsp_motor.c/h)
- `Motor_Init`: 安全启动顺序(方向脚先滑行 → CCR清0 → 启动PWM)
- `Motor_SetSpeed(motor, speed)`: 单函数接口, speed范围-7200~+7200, 正负代表方向, 内部限幅
- `Motor_Stop`(滑行) / `Motor_Brake`(刹车) / `Motor_StopAll`(急停)
- 方向切换安全时序: ①关PWM(CCR=0) → ②改IN1/IN2 → ③恢复PWM
- 引脚: PWMA=PB1(TIM3_CH4), PWMB=PB0(TIM3_CH3), AIN1/2=PB10/PB11, BIN1/2=PB12/PB13

### 2. 灰度驱动 (BSP/bsp_gray.c/h)
- CD4051模拟开关选通道: AD0=PA2(bit0), AD1=PA3(bit1), AD2=PA4(bit2), OUT=PA5
- `Gray_ReadAll`: 8路数字量, 1=黑线(灯亮), 0=白底(灯灭)
- `Gray_GetPosition`: 加权平均位置 -7000~+7000(负=线偏左, 正=线偏右), 9999=全白丢线, -9999=全黑十字
- 通道切换后有50us稳定延时(必须保留, 见"踩过的坑"第6条)

### 3. 调试基础设施
- 串口printf输出, 波特率115200
- 烧录文件: `project/code/smart/MDK-ARM/f103/f103.hex`
- 开发日志 `project/docs/开发日志.md`, 调试记录 `project/docs/调试记录.md` 持续更新

### 4. Git提交历程
```
1f52c51 feat: 适配D153C双路TB6612驱动板，修复方向切换时序
546e23b feat: 添加八路数字灰度传感器驱动(bsp_gray)
a3eaf99 fix: 修正灰度传感器驱动(根据官方手册)
d72d791 fix: 修正main.c灰度测试注释(1=黑0=白)
362aed0 fix: 恢复灰度通道切换50us延时，修复通道1/5互换
3d868ca docs: 补全8.23灰度通道错位调试记录
```

## 四、卡住的问题

### 1. no-negative-echo工具安装被沙箱拦截
- TRAE沙箱拦截Python子进程写入 `C:\Users\Xiaodaidai\.agents\`, 安装器卡死
- Python 3.13.7已装好(`%LOCALAPPDATA%\Programs\Python\Python313\python.exe`)
- 安装包缓存在 `%TEMP%\nne-install-33712\no-negative-echo`(**可能已被系统清理**)
- 解决: 在TRAE外手动开PowerShell运行安装器; 若缓存目录已删, 重新clone `https://github.com/LB623/no-negative-echo` 按其README装

### 2. 同PWM下左右轮有差速(未根治)
- 根因: 电机个体差异, 死区不同(左轮约PWM>600才启动, 右轮约>400)
- 开环无法消除, 按现方案: 循迹PID会部分补偿; 彻底解决需编码器闭环

## 五、下一步计划

1. **循迹控制代码**(下一个任务): 主循环读 `Gray_GetPosition()` 偏差 → 差速输出左右轮PWM
   - 黑线偏左(pos<0) → 左轮减速、右轮加速(车左转); 偏右反之
   - 建议先纯P控制: `转向量 = Kp * pos`, 左轮=基础速度-转向量, 右轮=基础速度+转向量
2. **参数整定**: 基础速度先给1500~2000(高于死区), Kp从小往大调, 观察蛇形振荡
3. **丢线处理**: pos=9999时保持上次转向方向找回线, 或缓停
4. **进阶(可选)**: 编码器速度闭环PID、刹车停车策略、全黑十字线识别

## 六、未完成的事

- [ ] 循迹控制代码(可新建BSP/bsp_follow.c/h或先写在main.c)
- [ ] no-negative-echo工具安装(需沙箱外执行, 见第四节)
- [ ] 编码器硬件+软件(远期, 用于根治电机差速)
- [ ] 循迹参数(Kp/基础速度)整定

## 七、踩过的坑

1. **TB6612 PWM脚悬空** → 电机抽搐/抖动, PWM线必须接牢
2. **TB6612真刹车条件**: IN1=1, IN2=1 且 PWM=满占空比; PWM=0时(1,1)只是高阻滑行, 不是刹车
3. **驱动板VM/GND反接 → 烧板**(已烧一块旧D24A), 上电前务必万用表量极性
4. **电机死区**: PWM低于200~800不转(静摩擦+齿槽转矩), 开环测试给2000+才可靠
5. **灰度AD0/AD2接反** → 8路全乱序(挡1~8号灯亮的位置是5,1,3,7,2,6,4,8); 排查时先核接线再查代码
6. **灰度通道切换后必须延时50us再读OUT**: 延时被注释导致通道1/5互换(LM393比较器未稳定就读了), 此延时永远不要删
7. **电机换向前必须先关PWM**: 绕组是电感电流不能瞬变, PWM开着直接翻IN1/IN2会产生数倍堵转电流的尖峰(管子发热、电源电压跌落、机械冲击), 三步顺序不能乱
8. **BOOT0跳线必须接GND**, 否则无法正常启动/下载

## 八、我的偏好(协作约定)

- **中文沟通**
- **驱动代码我自己写**: AI提供思路、指导、答疑, 不直接给完整代码
- **修改日志/调试记录前必须先征得我同意**; 我问"能不能改"时等确认, 不要直接动手
- **C代码风格**: 大括号独占一行(`{`比if缩进4格), 代码体再缩进4格, `}`与`{`对齐, `else if`与`if`对齐; 运算符两侧加空格; 尾注释用`/* */`
- **自动commit**: 每完成一个功能或值得记录的修改就commit一次, 不用提醒
- **commit风格**: `feat:/fix:/docs:` + 中文描述
- 硬件接线改动后, 优先怀疑接线再怀疑代码(本次通道乱序就是接线+时序双重问题)

## 九、硬件接线速查

| 模块 | 信号 | STM32引脚 |
|---|---|---|
| D153C电机A(左轮) | PWMA / AIN1 / AIN2 | PB1 / PB10 / PB11 |
| D153C电机B(右轮) | PWMB / BIN1 / BIN2 | PB0 / PB12 / PB13 |
| D153C供电 | VM=7.2~12V电池, 5V输出给STM32, STBY跳线帽接3.3V | — |
| 灰度传感器 | AD0 / AD1 / AD2 / OUT | PA2 / PA3 / PA4 / PA5 |
| 灰度供电 | VCC必须5V(不是3.3V), 探头离地约18mm | — |
