# 项目交接文档 (Handoff)

> 更新时间: 2026-09-12
> 项目: 基于 STM32F103C8T6 的智能循迹小车
> 代码位置: `project/code/smart/` (Keil MDK 工程, Target: f103)
> 远程仓库: `https://github.com/Li1433223Kai/stm32-smart-car.git` (main 分支, 已同步)
> 最新提交: `40cac75`（docs: 补全项目总结.md）

---

## 📌 当前状态速览（2026-09-11）

**实车验收进展（2026-09-12）**
1. **刹车失效修复 ✅ 实车验证通过**——原代码的刹车调用既是**死代码**、又会被同拍速度环覆盖；改用 `s_brake` 保持标志 + 转移当拍执行动作（方案 A）。判据：LOST 下快甩轮子松手，轮子立即被电磁制动拽停（注意排查过程中曾误判"失效"，根因是旧固件未烧录，见关键坑 9）
2. **显示快照停在旧值**——丢线后串口/OLED 看不到 `9999/-9999` 哨兵，停车后仍显示旧转速；改为每拍更新（旧固件曾表现出"LOST 显示 7000"的指纹，重烧后待复测确认 `pos:9999`）
3. **每拍重复读两次灰度**——改为把 pos 传进位置环，省约 0.5ms/拍，且状态判断/控制/显示用同一个采样值
4. **编码器 Δt 测速改造**——`Encoder_UpdateSpeed` 改用实测采样间隔换算 RPM，拆除"固定 10ms 周期"假设（打印/显示等拖慢节拍时测速不再失真）；`CONTROL_TICK_MS` 与 `ENCODER_SAMPLE_MS` 解耦（后者已删除）

**未修的问题**
- 全黑（CROSS）当停车处理（赛道上更常见的做法是直行冲过去）
- 刹车无限期保持（可选：保持 N ms 后转滑行）
- 参数风险未验证：转向增益 `KP×MAX_STEER` 从 50 提到 120（2.4 倍）有直线蛇形风险；`TARGET_MIN=40` 可能让弯道内侧轮跌入电机死区（左轮 PWM<600 不转）
- 右编码器 A/B 是否也反——未验证
- `项目总结.md` 第五节把刹车写成正常工作、第 85 行还把状态机标为 ✅ 已验证，与事实不符，待同步更正

**下一步（按优先级）**
1. ~~烧录实车验证刹车修复~~ ✅ 完成（2026-09-12，甩-松手判据；教训见关键坑 9）
2. 标准场地循迹验证 + 补量化性能数据（稳态误差/超调/调节时间/线速度）
3. OLED 新屏实测（取消 main.c 里被注释的刷新代码）
4. 采购 MPU6050 + 电位器（约 ¥15）
5. FreeRTOS 学习 + 用小车做重构（前置：NVIC 中断优先级）

> ⚠️ **工作区有未提交改动**：`app.c`/`app.h`（3 项修复）、`bsp_encoder.c`/`bsp_encoder.h`（Δt 测速）、`main.c`（OLED 注释 + CONTROL_TICK_MS 独立定义）、`handoff.md`、`项目约定.txt`；调试记录/开发日志/版本说明/项目总结的更新稿起草中

---

## 一、项目概况

- **主控**: STM32F103C8T6
- **驱动**: TB6612 双路（D153C）
- **电机**: MG310 直流减速电机（霍尔编码器，13 线，减速比 20）
- **传感器**: 8 路数字灰度
- **供电**: 12V 锂电池 → DCP3512 电源模块（4路3.3V/5V/12V/ADJ）→ 板子/电机/灰度
- **功能**: 白底黑线循迹小车，**速度环 PI + 位置环 P 双闭环**差速循迹
- **git 代理**: 本机需走 Clash `127.0.0.1:7897`（已配全局 http/https proxy）；Clash 没开时 push 会失败，临时取消用 `git config --global --unset http.proxy`（https.proxy 同理）

## 二、我做了什么（已完成）

### 1. 电机驱动 `bsp_motor.c/h`
- TB6612 双路，PWM(0~7200) + 方向控制
- 方向安全时序：先关 PWM → 改方向 → 恢复 PWM
- 引脚：PWMA=PB1, PWMB=PB0, AIN1/2=PB10/PB11, BIN1/2=PB12/PB13
- 接口：`Motor_Init / Motor_SetSpeed / Motor_Stop / Motor_Brake / Motor_StopAll / Motor_BrakeAll`

### 2. 灰度 `bsp_gray.c/h`
- 8 路数字灰度，加权计算黑线位置
- `Gray_GetPosition()`: -7000~+7000，0居中，负偏左，正偏右；9999全白，-9999全黑
- 通道切换必须等 50us（CD4051 切换 + LM393 比较器稳定）

### 3. 编码器测速 `bsp_encoder.c/h`
- TIM2(左)/TIM4(右)，4倍频，每圈脉冲数 = 20×13×4 = 1040
- 10ms 采样算 RPM
- 引脚：左 A=PA1/B=PA0，右 A=PB6/B=PB7

### 4. 控制层 `app.c/h`（核心）
- **位置环（外环）** `App_Position_Control(int16_t pos)`：用传入的黑线位置做 P 控制，算左右目标速度(RPM)，差速转向（pos 由状态机传入，每拍只读一次灰度）
- **速度环（内环）** `APP_Speed_Control()`：编码器反馈 → PI 控制 → PWM
- 速度环参数：`PID_KP=15, PID_KI=1, INTEGRAL_MAX=2000, PWM_LIMIT=7200`
- 位置环参数：`KP=1.5, BASE_SPEED=120, TARGET_MIN=40, TARGET_MAX=200, MAX_STEER=80`（2026-09-04 调整）
- 转向量：`steer = KP * (pos/7000) * MAX_STEER`
- **状态机**：`App_State_Update()` 每 10ms 调度 IDLE/RUN/LOST/CROSS。IDLE 停车按 PA8 启动；RUN 正常压线跑位置环、全白→LOST、全黑→CROSS；LOST/CROSS 在**转移当拍**就 `App_Stop()` + `Motor_BrakeAll()` 并置 `s_brake=1` 保持刹车（速度环不再输出），**按 PA8 可回到 RUN 重新起步**（不用断电重启）

### 5. VOFA+ 调参 / 串口调试
- 串口 115200，USART1(PA9/PA10)
- printf 用纯 CSV `%d,%d\n`（FireWater），带标签会只出一个通道
- 串口阻塞已修：fputc 超时 50ms（不死等）+ 打印限频 500ms

### 6. 9.1 小改进
- **编码器引脚上拉**：PA0/PA1/PB6/PB7 改 `GPIO_PULLUP`（tim.c + f103.ioc 同步）
- **刹车**：新增 `Motor_BrakeAll()`。⚠️ 该功能原先**从未生效**（死代码 + 同拍被速度环覆盖），2026-09-11 已修复，详见第四节
- **按键恢复**：LOST/CROSS 按下 PA8 回 RUN

### 7. 文档体系（已全部完成）
- `README.md`（2026-09-11 重写）
- `docs/`: **项目总结.md**（2026-09-11 完成，含实测状态表与后续规划）、调试记录.md（19 条）、模块说明.md、硬件连接.md（含引脚总表）、项目参数.md、版本说明.md（v1.0~v1.4）、开发日志.md
- 根目录：`handoff.md`（本文件，AI 维护）、`项目约定.txt`（含第九节"改代码与验收原则"）

---

## 三、我的目标

- **短期**：把循迹小车做成能体现工程能力的项目（文档+功能），作为**嵌入式软件**方向的简历/面试素材
- **中期**：先精软件（单片机开发，STM32→Linux 驱动），找嵌入式软件工作
- **长远**：T 型发展，软件为主、逐步补硬件能力

---

## 四、⚠️ 改过但未实车验证 / 发现的逻辑问题（重要）

### 1. ⚠️ 刹车失效（**代码已修复 2026-09-11，实车未验证**）

**原代码有两个缺陷，叠加导致刹车完全没工作**：
1. **刹车调用是死代码**：进入沿检测 `if (s_prev_state != APP_STATE_LOST)` 恒为假——状态转移发生在 RUN 分支内部，而 `s_prev_state` 在函数末尾才更新，所以下一拍进 LOST 分支时 `s_prev_state` 已等于 `s_state`，`Motor_BrakeAll()` **从未被执行过**
2. **同拍覆盖**：即使被调用，`main.c` 顺序 `App_State_Update() → APP_Speed_Control()` 会让速度环的 `Motor_SetSpeed()` 重写方向引脚与 PWM 覆盖刹车；且进入那拍不清 target，速度环按 RUN 残留目标继续驱动

**修复方式（方案 A：app.c 内加刹车保持标志）**：
- 新增 `s_brake` 标志；**状态转移动作统一放到 `if (s_state != prev_state)` 处执行**（转移动当拍生效，必然早于本拍速度环）：`App_Stop()` 清目标+积分 → `Motor_BrakeAll()` → `s_brake = 1`
- `APP_Speed_Control()` 开头加 `if (s_brake) { Motor_BrakeAll(); return; }`——速度环不再输出，刹车不会被覆盖
- 离开 LOST/CROSS（按 PA8 回 RUN）时清除 `s_brake`
- 顺带删掉失效的 `s_prev_state`；LOST 与 CROSS 合并为一个 case（行为一致）

**状态**：ARMCC 单文件编译 0 错误；**实车未验证**（需确认丢线后是否真的快速停住，用手转轮子感受短接阻力）。

**注意**：`docs/项目总结.md` 第五节把该刹车描述为正常工作（第 85 行还把状态机标为 ✅ 已验证），与以上不符，需同步更正。

### 2. 参数调整（KP1.5/BASE120/MIN40/STEER80）**未实车验证**
- 目的：解决 60cm 半径半圆冲出线（车速 150RPM≈0.51m/s 时该弯约需 38RPM 内外轮差速）
- 风险①：转向增益 `KP×MAX_STEER` 从 50 提到 120（**2.4 倍**），直线循迹有蛇形风险
- 风险②：`TARGET_MIN=40` 可能让弯道内侧轮目标跌入电机死区（左轮 PWM<600 不转）
- 硬约束：`BASE_SPEED − MAX_STEER ≥ TARGET_MIN`，否则限幅会截掉转向量

### 3. 待验证清单（按验收原则，未经实测一律标"未验证"）

| 项 | 状态 |
|---|---|
| 编译（ARMCC 单文件语法编译 app.c / main.c / tim.c） | ✅ 0 错误（其余警告：bsp_encoder.h 行尾缺换行；main.c 的 `last_oled_time`/`OLED_REFRESH_MS` 未引用——因 OLED 刷新代码被注释，属既有） |
| 状态机 IDLE→RUN、全白/全黑切换 | ⏳ 未验证 |
| LOST/CROSS 按 PA8 原地恢复 | ✅ 实测通过（2026-09-12） |
| 快速刹车（Motor_BrakeAll） | ✅ 实车验证通过（2026-09-12，甩-松手判据） |
| Δt 实测采样间隔测速 | ✅ 编译通过（2026-09-12），实车待回归对照（手转读数与改前一致 + 长间隔不失真） |
| 8 号灰度通道常亮 | ⚠️ 硬件隐患：白纸下仍报黑（pos=7000），真实赛道"全白"判定可能永不触发，待处理 |
| 编码器改上拉后计数准确 | ⏳ 未验证 |
| 位置环新参数循迹效果（60cm 弯） | ⏳ 未验证 |
| 直线不蛇形 | ⏳ 未验证 |
| OLED 显示 | ⏳ 待新屏实测 |
| 量化性能数据（稳态误差/超调/调节时间/线速度） | ⏳ 待测（见项目总结第七节） |

### 4. 逻辑复核中发现的其他问题

**已修 ①（2026-09-11）显示快照停在旧值**：显示快照原先只在 `App_Position_Control()` 里更新，导致进 LOST/CROSS 后串口与 OLED 仍显示最后一次 RUN 的 pos（**看不到 9999/-9999 哨兵值**），停车时转速也显示旧值。
改法：`s_display_pos` 移到 RUN 分支读到 pos 后立即记录（含哨兵）；转速快照移到 `App_State_Update()` 末尾每拍更新（所有状态都更新）；`App_Position_Control()` 不再写显示变量。

**已修 ②（2026-09-11）每拍重复读两次灰度**：RUN 分支读一次 pos 做状态判断，`App_Position_Control()` 内部又读一次（每次约 8×50us ≈ 0.5ms）。
改法：`App_Position_Control(int16_t pos)` 改为接收本拍已读的 pos；`app.h` 同步改签名并补 `#include "main.h"`（以获得 `int16_t`）。现在每拍只读一次灰度，节省约 0.5ms/拍。

**未改（等你定）**：

| # | 问题 | 影响 |
|---|---|---|
| 1 | **全黑（CROSS）当停车处理**：实际赛道里全黑多为十字线/起跑线，更常见的处理是"直行冲过去" | 与待做项"十字线识别"冲突，属功能取舍 |
| 2 | **刹车无限期保持**：停车期间电机两端持续短接（静止时无电流、可防溜坡，被推动时才有电流） | 可选优化：保持 N ms 后转滑行 |

---

## 五、接下来要做什么

### 1. 刹车修复的实车验证（最高优先级）
代码已改（见第四节第 1 条），需实车确认：
- 丢线/全黑后车轮是否**明显更快停住**（对比修复前）
- LOST 状态用手转轮子，应有明显短接阻力（不是自由滑）
- 按 PA8 能正常恢复跑；正常循迹与上电待机不受影响（回归）

### 2. 标准场地循迹验证 + 补量化数据
- 需白底黑线标准场地（灰度是固定阈值模块，地板不够白会误判）
- 用 VOFA+ 抓波形，测稳态误差 / 超调 / 调节时间 / 循迹线速度，回填 `项目总结.md` 第七节

### 3. OLED 新屏实测
- 旧屏烧了；`main.c` 里 OLED 刷新代码目前被注释（135-139 行），实测时取消注释
- 注意：CubeMX 重新生成 .uvprojx 可能清掉手工加入的 `bsp_oled.c/.h`，生成后需确认文件仍在工程里

### 4. 硬件采购清单（约 ¥15）
- **MPU6050 六轴模块** ¥5~10 —— 挂现有 I2C1（PB8/PB9，与 OLED 共用总线，地址 0x68 vs 0x3C），**0 新引脚**
- **10K 旋转电位器 ×1~2** ¥1~2/个 —— 接 PA6/PA7（ADC1_CH6/CH7，仅剩的 ADC 引脚），做在线调参

### 5. 学习 / 升级路线
- **FreeRTOS**：⚠️ 江协科技**没有** FreeRTOS 课程（官网仅 STM32/51/CAN，2026-09 核实）。主线改为：野火《FreeRTOS 内核实现与应用开发实战指南》（内核篇从 0 手写内核 + 应用篇，B 站配套视频，F103 例程可直接跟）→ 官方《Mastering the FreeRTOS Real Time Kernel》查漏 → 正点原子手册当工具书
  - 落地方式：把裸机超级循环重构成三任务（控制/串口/显示）；队列解决 printf 阻塞、互斥量保护共用的 I2C
  - **前置**：先补 NVIC 中断优先级（PendSV/SysTick 是调度基础）；**动工前先完成场地验证并 git tag 冻结基线**（不在未验收的版本上做架构重构）
- **陀螺仪姿态解算**：MPU6050 + 互补滤波（I2C 复用，0 新引脚）
- **主控升级（可选，暂不换板）**：引脚不够时选 RCT6（LQFP64 / 48K RAM，同族迁移只改引脚宏）或 F401 Black Pill（M4+FPU，姿态算法快）

### 6. 待做（晚点）
- **串口协议 + 上位机调参**：下发 PID/速度参数（建议 DMA + 空闲中断 + 环形缓冲，根治阻塞）
- **DMA 串口**：不定长接收协议
- **全黑十字线/起跑线识别**：复用 CROSS 检测
- **PC13 指示灯**：当前常亮不闪（主循环 toggle 太快，观察误判，非功能问题）
- **右编码器 A/B 是否也反**：未验证（左轮已确认 A=PA1/B=PA0）

---

## 六、关键坑（已解决）

1. 编码器 A/B 相接反 → 左 A=PA1/B=PA0 方向才对（右轮未验证）
2. 速度环 Kp=50 变速震荡 → 降到 15 才稳
3. 纯 P 有静差 → 加 Ki=1 消除
4. VOFA+ 带标签只出一个通道 → 用纯 CSV
5. 灰度地板不够白会误判（8号灯常亮）→ 此模块不能调阈值，需白底场地
6. TB6612 PWM 脚悬空会抖 / 驱动板反接烧板 / 电机个体差异（左轮死区>600）
7. printf 阻塞主循环 → fputc 改 50ms 超时 + 打印限频 500ms
8. 全黑/全白 pos=±9999 不能进位置环 → 先判哨兵值再跑控制（否则轮子疯转）
9. 改完代码 ≠ 板子上有这个代码 → 验收前先确认烧录版本（9.12 刹车"失效"实为旧固件，排查绕了一圈）
10. 灰度 8 号通道常亮（白纸下仍报黑）→ "全白丢线"判定可能永不触发；测试时"提离地面"才是可靠的全白触发

---

## 七、代码/文档速查

- 主程序：`Core/Src/main.c`（初始化 + 主循环，10ms 调 App_State_Update + 速度环）
- 控制逻辑：`BSP/app.c`（状态机 + 位置环 + 速度环 + OLED 面板 + 串口打印）
- 按键：`BSP/bsp_key.c`（PA8 启停，50ms 去抖 + 沿检测）
- 驱动：`BSP/bsp_motor.c`, `bsp_gray.c`, `bsp_encoder.c`, `bsp_oled.c`
- 文档：`project/README.md`、`project/docs/`（项目总结/调试记录/模块说明/硬件连接/项目参数/版本说明/开发日志）
- 约定：`项目约定.txt`（用户背景、项目主线、代码组织原则、第九节"改代码与验收原则"）

---

## 八、恢复上下文方法

下次直接说："读 handoff.md（和项目约定.txt），继续做 XXX"。

按当前进度，优先级建议：**实车验证刹车修复 → 实车验证参数 / 标准场地测量化数据 → OLED 实测 → FreeRTOS 重构**。
