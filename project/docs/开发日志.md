|**8.16**|

感觉自己没有一个独立完成的拿得出手的项目，于是打算利用现有的元件独立开发与一个智能小车，今日开始重温keil开发stm32，今天重新尝试一下基础配置，烧录，点灯，最终都正常，重温了cubemx的使用.

|**8.17**|
今天尝试在vscode配置keil插件，在vscode里写stm32代码，检测串口打印功能，屏幕显示功能。配置电机引脚，编写电机控制代码，组装电机和电机驱动和轮子，遇到问题未解决，买的小车底盘孔位有问题，电机底座挡住电机驱动孔

|**8.18**|
重新装了一下小车，4轮两驱，先拧顶级驱动模块的螺丝再装底座即可，组装了一下小车，装上了电机驱动版，四轮两驱，电源模块，c8t6，12v锂电池

|**8.19**|
优化了电机驱动代码，测试过程中电机驱动板0不小心反接烧坏了

|**8.21**|
换上了新的电机驱动，经过调试后，小车可以正常运行向前向后，但同pwm下还有些许差速现象.加上了灰度模块，移动了一下stm32在面包板的位置。

|**8.23**|
编写灰度模块的代码，测试灰度模块是否正常

|**8.24**|
编写简单循迹逻辑，加入单环位置pid5

|**8.25**|
加入编码器，编写简单读编码器值逻辑，重新组装了一下小车，见img/8.25重装后

|**8.27**|
编写速度环控制逻辑，调通速度环，并学习使用vofa+调参,完成了双环闭环设计

|**8.28**|
完善了到目前为止的文档整理编写，包括硬件连接，模块说明等，加入了i2c的0.96寸oled显示，但oled硬件损坏

|**8.29**|
加入了一个按键控制，准备用来控制状态切换

|**8.31**|
对循迹控制做了状态机重构：把原来散在 App_Position_Control 里的 if-else（全黑/全白/正常）改成显式状态机 IDLE/RUN/LOST/CROSS。上电默认 IDLE 停车，按 PA8 按键切 RUN 开始循迹，循迹中全黑切 CROSS、全白切 LOST 并停车。调通了按键启停和全黑/全白停车逻辑。

|**9.1**|
小改进：① 编码器引脚(PA0/PA1/PB6/PB7)改为上拉输入，信号线虚接时计数不乱跳（tim.c + f103.ioc 同步）；② 新增 Motor_BrakeAll() 双电机同时刹车；③ LOST/CROSS 刚进入时快速刹停一次、停留时 target=0 保持静止；④ LOST/CROSS 状态下按按键可回到 RUN 重新起步（不用断电重启），实现了丢线/十字停车后的手动恢复。