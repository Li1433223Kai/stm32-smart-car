|**8.16**|

感觉自己还没有一个独立完成、拿得出手的项目，于是打算用现有的元件独立做一台智能小车。今天开始重温 Keil 开发 STM32：重新试了一遍基础配置、烧录、点灯，都正常，顺便复习了 CubeMX 的用法。

|**8.17**|

今天尝试在 VSCode 里配置 Keil 插件、写 STM32 代码，测了串口打印和屏幕显示。配置电机引脚、写电机控制代码，组装电机、驱动板和轮子时遇到问题没解决：买的小车底盘孔位不对，电机底座把驱动板的孔挡住了。

|**8.18**|

重新装了一遍小车，四轮两驱。关键一步：先拧驱动模块的螺丝，再装底座。装好了电机驱动板、电源模块、C8T6 和 12V 锂电池。

|**8.19**|

优化了电机驱动代码，测试时不小心把驱动板电源反接，烧坏了一块。

|**8.21**|

换上新驱动板，调试后小车能正常前进后退了，但相同 PWM 下还有一点差速。加了灰度模块，把 STM32 在面包板上的位置挪了挪。

|**8.23**|

写灰度模块的代码，测它是否正常。

|**8.24**|

写简单的循迹逻辑，加入单环位置 PID（Kp=5）。

|**8.25**|

加入编码器，写了简单的读编码器逻辑，重新组装小车（见图 img/8.25重装后）。

|**8.27**|

写速度环控制逻辑并调通，学着用 VOFA+ 调参，完成了双环闭环设计。

|**8.28**|

整理了到目前为止的文档（硬件连接、模块说明等），加入 I2C 的 0.96 寸 OLED 显示，但 OLED 硬件损坏了。

|**8.29**|

加了一个按键，准备用来控制状态切换。

|**8.31**|

对循迹控制做了状态机重构：把原来散在 App_Position_Control 里的 if-else（全黑/全白/正常）改成显式状态机 IDLE/RUN/LOST/CROSS。上电默认 IDLE 停车，按 PA8 按键切 RUN 开始循迹，循迹中全黑切 CROSS、全白切 LOST 并停车。调通了按键启停和全黑/全白停车逻辑。

|**9.1**|

小改进：① 编码器引脚（PA0/PA1/PB6/PB7）改为上拉输入，信号线虚接时计数不乱跳（tim.c + f103.ioc 同步）；② 新增 Motor_BrakeAll() 双电机同时刹车；③ LOST/CROSS 刚进入时快速刹停一次、停留时 target=0 保持静止；④ LOST/CROSS 状态下按按键可回到 RUN 重新起步（不用断电重启），实现了丢线/十字停车后的手动恢复。

|**9.2~9.10**|

在学校学习 PCB，小车项目暂停。

|**9.11**|

排查发现刹车从未生效：刹车调用是死代码，且会被同拍速度环的 Motor_SetSpeed 覆盖。改用 s_brake 保持标志 + 转移当拍执行刹车动作，速度环遇到标志短路返回。

|**9.12**|

实车验收刹车修复：重新烧录固件后测试通过（快甩-松手轮子立即停转），LOST/CROSS 按 PA8 原地恢复实测通过。
编码器测速改为实测采样间隔 Δt，不再假设固定 10ms；CONTROL_TICK_MS 归 main.c 独立定义。
显示快照改为每拍更新；pos 传参进位置环，每拍少读一次灰度。
更正 bsp_motor.c 中 TB6612 真值表注释；旧 OLED 屏损坏期间，OLED 初始化与刷新保持注释。
